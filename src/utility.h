#pragma once

#include <vector>
#include <string>
#include <sstream>
#include <algorithm>

std::vector<std::string> split(std::string, char);
std::string toLower(const std::string&);
std::string replaceChar(std::string, char, char);
std::string trim(std::string);